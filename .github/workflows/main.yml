name: CI/CD - Build & Deploy to Render

on:
  push:
    branches: [ main ]  # Trigger when code is pushed to main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend          # 👈 Path to folder where Dockerfile lives
          file: ./frontend/Dockerfile  # 👈 Explicitly tell where Dockerfile is
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/react-ts-docker-demo:latest

      - name: Trigger Render Deploy
        run: |
          curl -X POST \
          -H "Accept: application/json" \
          -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
          https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys
